package com.ipnotica.menu.content.slider.thumbproduct.colors {		import flash.display.MovieClip;	import flash.events.Event;	import flash.events.MouseEvent;		public class ColorBox extends MovieClip	{						// INSTANCE		public var product:XML;						/********		 * INIT		 ********/				public function ColorBox(product:XML) {			super();			this.product = product;			init();		}				private function init():void {			initVisuals();			initEvents();		}						/**********		 * VISUALS		 **********/				private function initVisuals():void {			buttonMode = true;			initImage();		}				public function initImage():void {			trace("--> Setting the color to the desired one")		}								/*********		 * EVENTS		 *********/				private function initEvents():void {			addEventListener(MouseEvent.CLICK, onClickColorBox);		}				private function onClickColorBox(e:Event):void {			trace("--> You have clicked a product");			/*			this.visible = false; 			selected.visible = true;						Config.currentColor = colorIcon.color;			Config.body.content.update();						// redraw the stuff that changes based on color			var thumb:Thumb = Thumb(parent);			selected.setColor(Config.currentColor.@color);			Config.currentColor = currentColor;			thumb.image.reload();			Config.productToLoad = true;			Config.doc.dispatchEvent(new CustomEvents(CustomEvents.THUMB_CLICKED, {type: thumb.item.type, id: thumb.id, item: thumb.item}));*/		}			}}